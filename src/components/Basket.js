import React, { Component } from 'react';
import { NavLink } from 'react-router-dom';

export default class Basket extends Component {
  render() {
    return (
      <h1 className="title">This is the basket</h1>
    )
  }
}
